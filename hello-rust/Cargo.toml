[package]
 tokio = { version = "1", features = ["full"] }
    reqwest = { version = "0.11", features = ["json"] }
    scraper = "0.18" # Для парсинга HTML
    ratatui = "0.26" # TUI библиотека
    crossterm = "0.27" # Для работы с терминалом
    serde = { version = "1.0", features = ["derive"] }
    serde_json = "1.0"
name = "hello-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
